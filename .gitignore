# Secrets — never commit
.env

# Python
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/

# OS
Thumbs.db
.DS_Store

# Generated data is reproducible from src/run_all.py in ~4 seconds.
# Uncomment to keep the repo lean; left tracked here so a reviewer can open the
# dashboard without running anything first.
# data/processed/

# Timestamped pre-merge backups from the Data page's upload feature
# (src/data_upload.py) -- operational history, not a tracked deliverable.
data/raw/archive/
