pandas==2.2.3
numpy==2.1.3
duckdb==1.1.3
pyarrow==18.1.0
openpyxl==3.1.5
PyYAML==6.0.2
scipy==1.14.1
statsmodels==0.14.4
google-genai==2.18.1
python-dotenv==1.0.1
tabulate==0.9.0

# The dashboard is a static HTML site (src/build_site.py -> site/): hand-rolled
# inline-SVG charts, no charting or web-framework dependency, opens from disk
# with no server at all. src/serve.py adds a small HTTP layer only for the
# live AI assistant and the filter API, using the same
# data/processed/*.parquet tables as the rest of the pipeline.
#
# Validation is src/validate_site.py -- structural checks against the built
# site (figures, links, chart markup), run as the last step of
# src/run_all.py. There is no separate test suite.
#
# openpyxl backs the Data page's single "download all data" workbook -- every
# modelled table as its own sheet, generated at build time.
